package com.example.disaster_management;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class DisasterManagementApplicationTests {

	/**
	 * Basic sanity test to ensure that the Spring Boot application
	 * context loads successfully without errors.
	 */
	@Test
	void contextLoads() {
		// If application context fails to start, this test will fail.
	}
}
